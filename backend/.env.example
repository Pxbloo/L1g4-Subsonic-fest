# Archivo de ejemplo para las variables de entorno del backend. 
# Sustituye los valores por los que correspondan a tu entorno de desarrollo y renómbralo a .env
# fake (JSON local) o firebase
DATA_BACKEND=fake

# Opcional: si no usas la ruta por defecto backend/serviceAccountKey.json
FIREBASE_CREDENTIALS=../serviceAccountKey.json